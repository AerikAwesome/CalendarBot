version: 2.1

jobs:
  build:
    docker:
      - image: mcr.microsoft.com/dotnet/core/sdk:3.1
    steps:
      - checkout
      - run:
          name: Add nuget package sources
          command: dotnet nuget add source https://www.myget.org/F/quahu/api/v3/index.json -n "disqord"
      - run:
          name: "Install project dependencies"
          command: dotnet restore
      - run: 
          name: Build app
          command: dotnet build
